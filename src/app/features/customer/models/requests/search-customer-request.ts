export interface SearchCustomerRequest {
  nationalityId: string;
  customerId: string;
  accountNumber: string;
  firstName: string;
  secondName: string;
  lastName: string;
  orderNumber: string;
}
